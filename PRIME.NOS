#include<stdio.h>
#include<conio.h>
void main()
{
clrscr();
int num ,i, flag=0;
printf("enter the number :");
scanf("%d",&num);
for (i=2; i<num; i++)
{
	if(num%i==0)
	{
	flag=1;
	break;
	}
}
if(flag==0)
{
printf("Prime no");
}
else
{
printf("Not prime ");
}
getch();
}

